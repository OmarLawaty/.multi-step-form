type Step = 'your-info' | 'select-plan' | 'addons' | 'summary';

const STEPS: Step[] = ['your-info', 'select-plan', 'addons', 'summary'];

const App = () => {
  return <div>App</div>;
};

export default App;
